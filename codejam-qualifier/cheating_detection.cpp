#include <bits/stdc++.h>
#define lli long long int
using namespace std;

// to compile:
// g++ -std=c++11 -O2 -Wall cheating_detection.cpp -o cheating_detection

double sigmoid(double x)
{
    return exp(x) / (1 + exp(x));
}

int main()
{
    int T;
    int P;
    cin >> T;
    cin >> P;

    return 0;
}