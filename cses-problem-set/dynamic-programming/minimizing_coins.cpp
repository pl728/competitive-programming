#include <bits/stdc++.h>
#define lli long long int
using namespace std;

// to compile:
// g++ -std=c++11 -O2 -Wall _.cpp -o _

int main()
{
    int n, x;
    cin >> n;
    cin >> x;
    int c[n];
    for (int i = 0; i < n; i++)
    {
        cin >> c[i];
    }
    return 0;
}