#include <bits/stdc++.h>
#define lli long long int
using namespace std;

// choose any element a_k. 
// a_k and all elements equal to (a_k - 1) and (a_k + 1) will be deleted from the array. a_k points are earned. 

int main()
{
    int n; cin >> n;
    int a[n]; 
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }
    return 0;
}