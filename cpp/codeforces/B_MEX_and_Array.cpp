#include <bits/stdc++.h>
#define f(i,b) for (int i = 0; i < b; i++)
#define pb push_back
#define all(a) (a).begin(), (a).end()
#define v vector

using ll = long long;
using namespace std;

const int M = 1e9 + 7;
int dx[]={0,0,1,-1};
int dy[]={1,-1,0,0};
int grid[1005][1005] = {0};
int visited[1005][1005] = {0};

void solve() {
    int n; cin >> n;
    v<int> a(n);
    f(i,n) cin >> a[i];

}

int main() {
    int tc;
    cin >> tc;
    while(tc--) {
         solve();
    }
}