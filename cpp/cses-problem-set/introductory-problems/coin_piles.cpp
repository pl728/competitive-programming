#include <bits/stdc++.h>
#define lli long long int
using namespace std;

int main()
{
    int t; cin >> t;
    for(int i = 0; i < t; i++) {
        lli a, b;
        cin >> a >> b;
        // cases: 
        // a = 2b or b = 2a 
        // a == b and a % 3 == 0 
        if((a + b) % 3 == 0 || a )

    }
    return 0;
}